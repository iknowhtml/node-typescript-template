# Node TypeScript Template

A basic template for Node applications using TypeScript with linting.

#### To use

Checkout this repo, install dependencies, then run the following commands in the terminal.

```
> git clone https://github.com/iknowhtml/node-typescript-template.git
> cd node-typescript-template
> yarn install
```

Then run `yarn start` to start the application in development mode.
